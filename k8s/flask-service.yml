apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  selector:
    app: flask-app-pod
  type: NodePort
  ports:
    - name: http-port
      port: 8080
      targetPort: 5000
      nodePort: 32000
